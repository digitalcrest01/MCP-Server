{
  "name": "github-mcp-server",
  "version": "1.0.0",
  "description": "MCP server exposing GitHub repos, issues, pull requests, code search, and users to LLM agents via stdio.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "github-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "@octokit/rest": "^21.0.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18"
  }
}
